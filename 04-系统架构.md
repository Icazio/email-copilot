# 系统架构

## 1. 架构原则

本项目的架构原则是：

- `只读优先`
- `插件侧挂载`
- `不侵入现有系统`
- `人工闭环`
- `低成本、低维护`

## 2. 总体架构

```text
+--------------------+         +-----------------------+
|  Centuran 页面      |         |  其他后台网页          |
|  邮件线程/客服界面   |         |  客户/订单查询结果      |
+----------+---------+         +-----------+-----------+
           |                               |
           | DOM读取                        | 人工复制粘贴
           v                               v
+------------------------------------------------------+
|               浏览器插件侧边栏                        |
|  - 读取线程                                            |
|  - 展示摘要                                            |
|  - 展示字段                                            |
|  - 展示英文草稿                                        |
|  - 展示补问问题                                        |
|  - 展示下一步建议                                      |
+-------------------------+----------------------------+
                          |
                          | HTTPS
                          v
+------------------------------------------------------+
|                 Copilot 轻量后端                      |
|  - Prompt组装                                          |
|  - 模板注入                                            |
|  - 问题分类                                            |
|  - 字段抽取                                            |
|  - 草稿生成                                            |
+-------------------------+----------------------------+
                          |
                          | OpenAI API
                          v
+------------------------------------------------------+
|                 Responses API                         |
+------------------------------------------------------+
```

## 3. 前端架构

浏览器插件建议拆成三层：

### 3.1 Content Script

负责：

- 识别 Centuran 页面
- 抓取当前邮件主题、发件人、时间、正文、历史线程
- 将结构化文本传给插件侧边栏

### 3.2 Side Panel

负责：

- 展示 Copilot 结果
- 接收人工补充内容
- 二次生成
- 复制回复草稿

### 3.3 Background Script

负责：

- 路由请求
- 管理用户设置
- 管理模板缓存

## 4. 后端架构

后端建议保持极简：

- 一个 `API server`
- 一个 `template store`
- 一个 `prompt orchestration layer`

建议接口：

- `/analyze-thread`
- `/generate-draft`
- `/refine-draft`
- `/extract-fields`
- `/health`

首期不必引入：

- 任务队列
- 向量数据库
- 多代理编排
- 复杂权限系统

## 5. 数据流

### 首次生成

1. 插件读取当前邮件线程
2. 插件发给后端
3. 后端拼装：
   - 场景说明
   - 输出格式要求
   - 对应邮件模板
4. 后端调用模型
5. 返回：
   - summary
   - extracted_fields
   - draft_reply
   - clarification_questions
   - next_step

### 二次优化

1. 客服手工补充后台查询结果
2. 插件将补充信息连同原草稿一起提交
3. 后端基于新增事实重写草稿

## 6. 安全边界

为了满足“不要乱调系统”，架构上要明确做到：

- 插件不自动点击页面按钮
- 插件不自动填写和提交表单
- 后端不保存敏感长期状态
- 不连接内部写操作接口
- 所有输出只到插件侧边栏，不写回业务系统

## 7. 推荐技术选型

### 插件端

- `TypeScript`
- `Chrome Extension Manifest V3`
- `React` 或轻量 UI 框架

### 后端

- `Node.js` 或 `Python`
- 简单 REST API

### 模型接入

- `Responses API`

原因：

- 更适合多轮上下文处理
- 更适合后续扩展工具和结构化输出

## 8. 为什么不建议首期用重型方案

当前约束非常强：

- 只有 1 个人
- 不能改现有系统
- 不能接内部 API
- 先求尽快上线

所以不建议首期做：

- 企业级知识库工程
- 大型工作流编排
- 复杂的 Codex 自动执行链路

这些都应放到后续版本。
