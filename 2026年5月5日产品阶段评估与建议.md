# 产品阶段评估与建议

> 评估日期：2026-05-05

---

## 当前所处阶段：内部 Alpha

| 阶段 | 特征 | 是否到达 |
|------|------|---------|
| Prototype | 核心链路能跑通 | ✅ |
| Alpha | 内部可用，有真实数据输入 | ⚠️ 刚到 |
| Beta | 小范围真实用户试用 | ❌ |
| GA | 稳定、可扩展、可监控 | ❌ |

项目已具备完整的主流程（邮件捕获 → 分析 → 草稿生成 → 精修），但尚未经过真实用户验证。

---

## 现阶段最重要的问题

**不是技术问题，是验证问题。**

这个产品的核心假设是：

> AI 生成的草稿能帮客服节省时间、提升回复质量。

这个假设目前没有被真实数据验证过。在这个阶段，最危险的事情是在一个还没有被用户证明有价值的方向上过度投入工程。

---

## 关于"优化识别流程"的判断

**现阶段不建议做。**

原因如下：

- **没有数据支撑方向。** 不知道识别在真实邮件上的准确率是多少、哪类邮件最容易误判、误判的代价有多大。在没有数据之前优化算法，是在猜测方向。
- **识别准确率不是现在最贵的问题。** 就算识别从 70% 提升到 90%，如果客服觉得工作流不顺畅，或草稿风格与公司规范不符，他们就不会用这个工具。
- **应等 Pilot 数据出来后再决定。**

---

## 行动建议

### 第一优先：运行 2 周内部 Pilot

找 2-3 名真实客服，用这个工具处理约 100 封真实工单。重点观察：

- 草稿被采用率（直接用 / 改了再用 / 完全不用）
- 哪类场景最有价值，哪类场景表现最差
- 客服在哪个步骤最费劲、最容易放弃

**Pilot 前唯一值得先做的技术事项：** 加上请求日志（已完成），这样可以回溯"这封邮件是怎么被处理的"。

### 第二优先：在 Pilot 开始前锁定成功指标

不定义指标就开始迭代，产品方向会漂移。建议现在确认：

| 指标类型 | 具体定义（示例） |
|----------|----------------|
| 效率指标 | 起草一封回复的时间从 X 分钟降到 Y 分钟 |
| 质量指标 | 草稿采用率 > 60%（直接用或小改） |
| 覆盖指标 | 能有效处理 > 80% 的工单类型 |

### 第三优先：验证产品形态本身

当前设计是"侧边栏 + 手动触发"。有一个潜在的摩擦点需要在 Pilot 中重点观察：

> 客服每次需要手动点击扩展图标，切换注意力到侧边栏，再把草稿复制回邮件输入框。

这个动作链路是否流畅，是产品成败的关键，比识别准确率更重要。

---

## Pilot 结束后的迭代优先级（参考）

Pilot 数据出来后，按以下逻辑决定下一步：

- 如果草稿采用率低 → 优先优化草稿质量（切换真实 LLM 或改进模板）
- 如果工作流摩擦大 → 优先改进 UI 交互（自动触发、一键插入草稿）
- 如果特定类别误判多 → 再考虑优化识别流程
- 如果覆盖的工单类型太少 → 扩展模板和场景

---

## 一句话总结

> 现在是"验证价值"阶段，不是"优化算法"阶段。先让真实用户用起来，用数据决定下一步优化什么。

---

## 产品实现逻辑与处理链路

> 记录日期：2026-05-05

整个流程分为 6 个步骤，分布在扩展和后端两侧。

### 第一步：捕获邮件线程（Capture）

客服点击"Capture"按钮后，`panel.js` 做了以下事情：

1. **查询当前标签页的所有 frame**（包括主页面和嵌套 iframe），向每一个 frame 都发送 `EMAIL_COPILOT_EXTRACT` 消息
2. **`content.js` 在每个 frame 里独立执行**，先判断是否是 Znuny/OTRS 工单页面（检查 `#ArticleTableBody` 是否存在，或 URL 是否含 `AgentTicketZoom`）
3. **Znuny 专用路径**（当前系统走这条）：
   - 读取工单文章列表（`#ArticleTableBody tbody tr`），拿到每封邮件的 `article_id`、发件人、时间、方向
   - **并行发起网络请求**，用浏览器的登录 Cookie 去抓每封邮件的完整 HTML（`/otrs/index.pl?Action=AgentTicketArticleContent`）
   - 解析 HTML，提取 `.ArticleBody` 正文文本
   - **清洗文本**：去掉 "Dear xxx," 开头、去掉签名档、去掉社交媒体链接
   - **去重引用历史**：检测 "On X wrote:" 模式，或与前一封邮件内容前缀重叠，截断引用部分，避免同一段话在多封邮件里重复出现
   - 还会顺手收集：客户邮箱地址、页面上的"标准回复"下拉列表选项
4. **多 frame 竞选最佳结果**：按 `正文字数×10 + 面板数×100` 评分，取分最高的那个 frame 的结果
5. **捕获完毕后立即在后台预热订单查询缓存**（`prefetchOrderSearchContext`），为下一步加速

### 第二步：自动订单查询（Auto Order Search）

捕获到客户邮箱后，无需人工操作，自动执行：

1. 先 GET `/otrs/index.pl?Action=AgentOrderSearch`，拿到页面里的 `ChallengeToken`（防 CSRF 的 token）
2. 再 POST 到订单查询接口，把客户邮箱作为查询条件提交
3. 解析返回的 HTML，按优先级尝试三种解析策略：
   - 正则解析 "Details of Order" 区块（最准确）
   - HTML 表格 thead/tbody 解析
   - `<dl>/<dt>/<dd>` 键值对解析
4. 结果格式化成纯文本填入"Manual Lookup"框，供后续步骤使用
5. 结果缓存 5 分钟，防止重复查询同一客户

### 第三步：分析线程（Analyze）

客服点击"Analyze"，`panel.js` POST 到后端 `/analyze-thread`：

后端用正则关键词匹配判断类别：
- `invoice / receipt` → 发票类
- `refund / money back / auto-renew` → 退款类
- `license code / registration code` → 激活码类
- `crash / not working / error` → 技术问题类
- 其余 → general（兜底）

同时提取结构化字段（order_id、邮箱、产品名），根据缺失字段生成追问问题，匹配建议模板。

返回：`{ category, extracted_fields, summary, clarification_questions, next_step, suggested_template_id }`

### 第四步：生成草稿（Generate Draft）

客服点击"Generate Draft"，POST 到 `/generate-draft`。

后端根据 `template_ids` 加载对应模板（如 `refund_auto_renewal_v1.json`），用提取出的字段填充占位符，拼装出完整草稿回复。

### 第五步：精修草稿（Refine，可选）

如果第二步的订单查询有结果，或者客服手动填写了备注，点击"Refine"，POST 到 `/refine-draft`。

后端把订单查询结果（实际订单号、购买日期、金额等）合并进草稿，生成更精准的版本。

### 第六步：复制发送

客服点"Copy"，草稿复制到剪贴板，手动粘贴进 Centuran 的回复框，审阅后自行发送。工具本身不会触发任何发送动作。

### 整体链路图

```
[Centuran 工单页面]
       │
       │ 1. Capture（抓取所有 frame）
       ▼
[content.js] ──网络请求──▶ Znuny 文章 HTML
       │ 清洗 / 去重引用
       ▼
[panel.js] ──自动──▶ 订单查询（POST AgentOrderSearch）
       │                    │
       │                    ▼ 订单信息填入 manualLookup
       │
       │ 2. Analyze
       ▼
[backend /analyze-thread] ──正则/LLM──▶ 类别 + 字段 + 建议模板
       │
       │ 3. Generate Draft
       ▼
[backend /generate-draft] ──模板填充──▶ 草稿
       │
       │ 4. Refine（可选）
       ▼
[backend /refine-draft] ──合并订单信息──▶ 精修草稿
       │
       │ 5. Copy → 人工审阅 → 手动发送
       ▼
[Centuran 回复框]
```
