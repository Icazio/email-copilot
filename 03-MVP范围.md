# MVP 范围

## 1. MVP 定义

本 MVP 不是一个自动化客服系统，而是一个 `浏览器插件形态的邮件回复辅助工具`。

其核心是：

- 读取当前邮件线程
- 帮客服理解上下文
- 基于模板生成英文回复草稿
- 提示补问问题和下一步建议

## 2. 必做范围

### 2.1 场景范围

MVP 只覆盖以下三类场景：

1. `订单/退款/发票`
2. `账号/权限`
3. `程序问题收集与升级`

### 2.2 功能范围

MVP 必做能力：

- 读取当前邮件线程
- 识别问题类型
- 提取关键字段
- 输出 case summary
- 输出英文回复草稿
- 输出补问问题
- 输出下一步建议
- 支持人工补充查询结果后再次生成

### 2.3 数据输入范围

MVP 允许以下输入方式：

- 自动抓取当前页面邮件线程文本
- 客服手工粘贴订单/客户查询结果
- 客服手工粘贴模板或补充说明

### 2.4 模板范围

MVP 使用现有固定邮件模板作为主要知识来源。

建议首期整理以下模板包：

- 退款类模板
- 发票类模板
- 续订取消类模板
- 激活码/注册码问题模板
- 程序问题过渡回复模板

## 3. 不做范围

MVP 不做：

- 自动发送邮件
- 自动写回 Centuran
- 自动打开或提交 Redmine
- 自动操作技术支持平台
- 自动执行退款或账号变更
- 自动拉取订单信息
- 自动访问内部系统 API
- 建立复杂 RAG 知识库
- 训练专有模型

## 4. 推荐的最小实现

为了让 1 个人尽快上线，建议 MVP 采用最小实现：

### 插件端

- 内容脚本读取邮件线程 DOM
- 侧边栏展示结构化结果
- 支持客服点击复制草稿
- 支持客服粘贴订单查询结果进行二次优化

### 服务端

- 一个轻量后端
- 调用 `Responses API`
- 不做复杂多代理
- 不做数据库重依赖

### 知识层

- 先使用本地模板配置文件
- 先不做大型知识库

## 5. 版本分层

### MVP V1

- 邮件线程读取
- 问题分类
- 字段提取
- 英文回复草稿
- 补问问题
- 下一步建议

### V1.1

- 模板管理后台或本地配置页
- 常见问题一键插入模板
- 更稳定的字段提取

### V2

- 接知识库
- 接内部查询接口
- 接 Redmine 辅助升级
- 接账号处理平台辅助跳转

## 6. 上线判定

满足以下条件即可认为 MVP 可上线试用：

1. 插件能稳定读取主要邮件页面内容
2. 退款/发票/注册码问题三类邮件有可用草稿
3. 程序问题能输出正确的收集与升级建议
4. 客服能在 1 分钟内完成一次生成和复制
5. 不会触发任何外部写操作
