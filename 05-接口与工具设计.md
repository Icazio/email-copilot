# 接口与工具设计

## 1. 设计原则

由于首期不接内部 API，所谓“工具”主要是指 Copilot 自身的结构化能力和插件侧可调用的后端接口，而不是直接操作公司内部系统的执行工具。

## 2. 后端接口

### 2.1 `POST /analyze-thread`

用途：

- 对邮件线程做问题分类和摘要

输入示例：

```json
{
  "subject": "Refund request",
  "thread_text": "full email thread text",
  "language": "en"
}
```

输出示例：

```json
{
  "category": "refund",
  "summary": "Customer asks for a refund after auto-renewal.",
  "confidence": 0.92,
  "extracted_fields": {
    "order_id": null,
    "account": "example@email.com",
    "invoice_id": null,
    "product_name": null
  },
  "clarification_questions": [
    "Could you please share your order number?"
  ],
  "next_step": "Ask for order information before replying with the refund template."
}
```

### 2.2 `POST /generate-draft`

用途：

- 基于邮件线程和模板生成首版英文草稿

输入示例：

```json
{
  "category": "refund",
  "subject": "Refund request",
  "thread_text": "full email thread text",
  "manual_notes": "",
  "template_ids": ["refund_auto_renewal_v1"]
}
```

输出示例：

```json
{
  "draft_reply": "Hi, thank you for contacting us...",
  "clarification_questions": [
    "Could you please share your order number?"
  ],
  "next_step": "Wait for the order number and verify the purchase details manually."
}
```

### 2.3 `POST /refine-draft`

用途：

- 客服补充人工查询结果后，重写草稿

输入示例：

```json
{
  "category": "refund",
  "thread_text": "full email thread text",
  "manual_lookup_result": "Order ID: 123456, purchased on 2026-04-20, auto-renewed",
  "previous_draft": "Hi, thank you for contacting us..."
}
```

输出示例：

```json
{
  "draft_reply": "Hi, thank you for the details. We have checked your order...",
  "next_step": "Agent should review refund policy before sending."
}
```

### 2.4 `POST /extract-fields`

用途：

- 独立提取关键信息字段

输入示例：

```json
{
  "thread_text": "full email thread text"
}
```

输出示例：

```json
{
  "order_id": "123456",
  "account": "customer@example.com",
  "invoice_id": null,
  "product_name": "Product A",
  "issue_type": "license_code_not_working"
}
```

## 3. 插件端能力

### 3.1 页面读取能力

插件应支持：

- 读取当前邮件主题
- 读取当前邮件正文
- 读取历史线程
- 识别客服当前打开的是哪封邮件

### 3.2 人工补充能力

插件应支持客服手工粘贴：

- 客户查询结果
- 订单查询结果
- 备注
- 模板补充说明

### 3.3 输出能力

插件应支持：

- 一键复制英文草稿
- 一键复制补问问题
- 一键复制下一步建议

## 4. 模板设计

由于没有可信主知识库，首期应把模板做成结构化配置。

建议模板字段：

```json
{
  "id": "refund_auto_renewal_v1",
  "scene": "refund",
  "sub_scene": "auto_renewal",
  "language": "en",
  "opening": "Hi, thank you for contacting us.",
  "body_guidance": [
    "Acknowledge the request.",
    "Do not promise a refund directly.",
    "Ask for the order number if missing."
  ],
  "closing": "Please let us know if you need any further assistance."
}
```

建议首期模板包：

- `refund_no_reason`
- `refund_auto_renewal`
- `refund_wrong_purchase`
- `refund_unsatisfied`
- `refund_slow_speed`
- `invoice_request`
- `cancel_renewal`
- `license_code_not_working`
- `program_issue_collect_info`

## 5. Prompt 设计

首期建议拆成三段 prompt：

### 5.1 系统指令

约束模型行为：

- 你是邮件客服 Copilot
- 只能生成内部建议
- 不得代表公司作出未经确认的承诺
- 遇到退款、补偿、SLA、技术根因时要保守

### 5.2 场景指令

针对问题类型注入：

- 退款类
- 发票类
- 激活码类
- 程序问题升级类

### 5.3 模板指令

注入已有邮件模板：

- 开场方式
- 常见措辞
- 必须补问的事实
- 禁止承诺的内容

## 6. 结构化输出建议

建议统一让模型输出如下 JSON：

```json
{
  "category": "",
  "summary": "",
  "extracted_fields": {
    "order_id": "",
    "account": "",
    "invoice_id": "",
    "product_name": "",
    "issue_type": ""
  },
  "draft_reply": "",
  "clarification_questions": [],
  "next_step": ""
}
```

好处：

- 插件可直接渲染
- 方便评测
- 方便后续接模板路由

## 7. 未来可扩展工具

这些不在 MVP 内，但接口设计上可以预留：

- `lookup_order`
- `lookup_customer`
- `create_redmine_ticket_draft`
- `generate_account_support_checklist`

注意：

- 这些未来工具也建议先做 `建议型`，不要直接写系统。
